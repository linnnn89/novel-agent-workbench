# 当前交接 / Current Handoff

日期：2026-08-17。

## 产品形态

给用户的程序是 `dist\NovelAgentWorkbench\`：

- `NovelAgentWorkbench.exe`：现代桌面界面
- `_internal\`：运行时依赖
- `用户数据\`：作品、设置、密钥。重新打包必须保留

源码运行入口：`novel-agent-workbench-desktop` → `modern_desktop.py`。未安装 `pywebview` 时回退经典 Tk。

## 现代界面覆盖

- 三栏工作台：作品树、稿纸、检查器
- 外观：浅色/深色、字体、字号、专注模式
- 模型设置：接入商、模型目录、功能分配
- 记忆库：勾选已确认章节、保存、生成、压缩
- 大纲与章节、世界观与人物：新建/编辑/删除，保存不联网
- 草稿：自动保存、重新生成、AI 审稿、精修、确认稿、导出 TXT

作品树已恢复右键菜单：打开、审稿/初审/重写/精修/确认、删除作品或章节、查看生成上下文。

已迁入：创作设置（全局/项目）、生成前预览格式、思考过程面板、确认后自动记忆总结、作品概览、清空回收站、关于软件。

仍在经典界面：

- 审稿与改写总表、已确认章节独立列表
- 模型连接检查、模型调用记录、运行记录
- 出稿清单、导出设置、使用说明、开发者诊断
- 参考作品与风格（原版也标了开发中）

## 安全边界

- 保存设置、打开作品、编辑正文不会自动联网
- 生成、审稿、精修、刷新模型、记忆生成需要用户明确点击
- 草稿必须显式确认才成为确认稿
- 密钥不进日志和元数据正文

## 构建

```cmd
BUILD_NovelAgentWorkbench.bat
```

脚本只替换 `NovelAgentWorkbench.exe` 和 `_internal`，不删除 `用户数据`。

## 仓库怎么读

- 当前代码：`src/novel_agent_workbench/`
- 用户包（不入库）：`dist/NovelAgentWorkbench/`
- 契约：`codex_docs/`
