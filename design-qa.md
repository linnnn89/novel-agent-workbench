# Design QA

## 最新验收对象：KIMI UI 合并

- 候选源码：外部 KIMI 沙箱副本中的 `src/novel_agent_workbench/desktop_app.py`（未提交该沙箱）。
- 候选视觉截图：外部 KIMI 沙箱的本地 QA 截图（未提交）。
- 当前项目源码：`src/novel_agent_workbench/desktop_app.py`。
- 当前项目最终 EXE 与功能菜单截图：本地 QA 临时文件（未提交）。
- 并排对照：本地 `work/test_outputs/` 测试产物（已由 `.gitignore` 排除）。
- 验证状态：Windows 桌面、125% 系统缩放、1920 × 1080 物理像素、最终 PyInstaller EXE。

## Findings

- 无未解决的 P0、P1 或 P2 问题。
- [P3] Tkinter 原生控件仍保留系统方角和有限的图标能力；这是框架限制，不影响本轮视觉迁移。
- [P3] 当前项目截图中的测试作品未展开，而 KIMI 候选截图展开了章节树；这是交互状态差异，不是样式偏差。

## 源码合并证据

- 两个仓库 Git HEAD 相同：`5408abd7c4ae7669b328f6cf616ed6ebd2902b0d`。
- 排除 `.git`、虚拟环境、构建目录和测试输出后，双方只有 `desktop_app.py` 存在内容差异。
- 合并后两份 `desktop_app.py` 的 SHA-256 均为 `3EF839B30FA3E89BD5E415A6B3AC209483074DA370B473BD8E9720CFC72560A2`。
- KIMI 修改约包含 388 行新增、92 行调整，集中在视觉令牌、控件样式和主窗口留白；草稿、审稿、精修、确认和模型配置业务方法未替换。

## 五项视觉核验

- 字体与层级：统一使用 Microsoft YaHei UI 字体族，并为品牌、标题、章节、状态和说明建立集中字体令牌。
- 间距与布局：主窗口使用冷灰背景和三块白色工作区，面板间增加稳定留白；右栏继续保持紧凑，正文仍占主要宽度。
- 颜色：统一为冷灰应用底色、白色内容面、靛蓝主操作、绿色确认、低饱和状态胶囊和浅蓝选择态。
- 图像与资产：新增“墨”字品牌徽标是文本品牌标记；没有引入外部图片、下载资源或新依赖。
- 文案：保留现有中文业务名称和用户要求的“重新生成（随机）”“根据审稿意见精修”“模型设置”。

## 交互核验

- “重新生成（随机）”“根据审稿意见精修”“AI审稿”和“确认稿件”继续常驻正文区，并绑定原业务命令。
- 顶栏继续提供“模型设置”，API Key 的保存、读取、写入和掩码实现未修改。
- “功能”菜单的级联菜单缩进错误已修复；最终 EXE 实际打开后显示工作台、创作、编辑、资料库、模型、创作设置、定稿和帮助共 8 个分组。
- 作品树、正文区、右侧检查器、滚动条、输入框、下拉框、Notebook、进度条和弹窗文本区均获得统一样式。

## 比较历史

1. 第一至第三阶段建立三栏工作台、高 DPI 次级窗口、紧凑右栏和正文快捷动作。
2. KIMI 沙箱在该最新版副本上增加集中视觉令牌和控件样式，并修复功能菜单级联缩进。
3. 当前项目以文件级精确补丁吸收 KIMI 的唯一源码差异；源码哈希与候选版本一致。
4. 最终 EXE 在 1920 × 1080、125% 缩放下复验：布局、色彩、状态胶囊和快捷动作均正常。
5. 功能菜单交互复验：8 个菜单分组全部可见，之前只显示最后一个分组的问题消除。

## 验证

- Python 3.10：18 项单元测试通过。
- 项目 `.venv` Python 3.14：18 项单元测试通过。
- `desktop_app.py` 编译检查通过。
- PyInstaller `-SkipInstall` 构建通过；未安装或升级依赖。
- 最终 `NovelAgentWorkbench.exe` 启动并响应正常；构建过程保留 `dist/NovelAgentWorkbench/用户数据`。

## Open Questions

- 无 `USER_DECISION_REQUIRED`。

## Implementation Checklist

- [x] 隔离 KIMI 沙箱的真实修改文件。
- [x] 精确合并 UI 源码并核对 SHA-256。
- [x] 保留第三阶段紧凑右栏、快捷动作和模型设置入口。
- [x] 修复并实测全部功能菜单分组。
- [x] 测试、打包、截图与 README 更新。

final result: passed
