# novel_agent_workbench Package

This package contains the new implementation code.

Current MVP-0 module:

- `storage.py`: safe local project storage kernel, checkpoint ZIP creation, and reversible checkpoint restore.
